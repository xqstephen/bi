<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xy.bi.mapper.ChartMapper">

    <resultMap id="BaseResultMap" type="com.xy.bi.model.entity.Chart">
            <id property="id" column="id" />
            <result property="name" column="name" />
            <result property="goal" column="goal" />
            <result property="chartData" column="chartData" />
            <result property="chartType" column="chartType" />
            <result property="genChart" column="genChart" />
            <result property="genResult" column="genResult" />
            <result property="userId" column="userId" />
            <result property="createTime" column="createTime" />
            <result property="updateTime" column="updateTime" />
            <result property="isDelete" column="isDelete" />
    </resultMap>

    <sql id="Base_Column_List">
        id,`name`,goal,chartData,chartType,genChart,genResult,
        userId,createTime,updateTime,isDelete
    </sql>
    <update id="createTable">
        ${createTableSql}
    </update>

    <update id="insertData">
        ${insertSql}
    </update>
    <select id="queryData" resultType="java.util.Map">
        select * from chart_${chartId};
    </select>


</mapper>
